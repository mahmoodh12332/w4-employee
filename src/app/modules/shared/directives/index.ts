import {DocumentHostDirective} from './document-host.directive';

export const SharedDirectives = [
  DocumentHostDirective,
];

export default {
  SharedDirectives,
};

export * from './document-host.directive';
